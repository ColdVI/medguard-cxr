# MEDGUARD-CXR Decisions Log

This log is append-only. Record non-trivial design choices, safety decisions, and owner conflict resolutions here.

| Date | Phase | Decision | Rationale | Owner |
|------|-------|----------|-----------|-------|
| 2026-05-07 | 0A review fixes | Added explicit `__init__.py` files to all `src/medguard` subpackages. | Non-editable wheels must include every subpackage; explicit packages avoid relying on PEP 420 namespace behavior. | Codex |
| 2026-05-07 | 0A review fixes | Collapsed Python support to `>=3.10,<3.13` and removed the untested Python 3.13 dependency branch. | CI and Docker validate Python 3.11 only; shipping two unverified dependency matrices would create packaging risk. | Codex |
| 2026-05-07 | 0A review fixes | Kept the Docker GPU target as an intentional Phase 0A placeholder deferred until Phase 1. | GPU install and CUDA runtime validation belong with the first real training path, not the import-safe skeleton. | Codex |
| 2026-05-07 | 0A review fixes | Deferred `bitsandbytes`, `timm`, and v0.2 experiment tracking packages until their owning phases. | QLoRA, ablations, and MLflow/W&B are not used in Phase 0A; owners must pin them before first use. | Codex |
| 2026-05-07 | 0A review fixes | Kept `tests/test_imports.py` as a deliberate canonical-structure extension for CI smoke coverage. | Section 6 omits an import-smoke test, but the Phase 0A task explicitly requested one. | Codex |
| 2026-05-07 | 0A review fixes | Assigned `src/medguard/models/grounding.py` ownership to Codex with Claude review. | Section 6 includes the file but Section 2 omits ownership; this aligns it with Phase 3 localization work. | Codex |
| 2026-05-07 | 0A review fixes | Assigned `notebooks/02_baseline_results.ipynb` ownership to Codex with Claude review. | The notebook is populated by Phase 1 baseline results and needs an explicit owner. | Codex |
| 2026-05-07 | 0A review fixes | Documented the Phase 0A placeholder convention: `PHASE` plus `is_available() -> False`. | The temporary skeleton API is explicit until each owning phase replaces or removes it. | Codex |
| 2026-05-07 | 1 | NIH transform returns 3-channel tensors when `preprocessing.normalization.active=imagenet`. | DenseNet121 ImageNet weights require 3 input channels; this follows the Section 4 normalization rule despite the older `[1,224,224]` return-shape note. | Codex |
| 2026-05-07 | 1 | `pos_weight` is computed from the Phase 1 training split until Phase 0B audit recommendations exist. | The data audit is not available yet, and the master prompt explicitly allows inline computation as a temporary fallback. | Codex |
| 2026-05-07 | 1 | Training/evaluation use deterministic smoke mode when local NIH metadata is missing. | The repository cannot assume dataset availability; smoke mode proves code paths without committing or synthesizing patient data. | Codex |
| 2026-05-07 | 1 | DenseNet121 uses ImageNet weights only when `model.allow_weight_download=true`; default is no download. | Local and CI smoke runs must be network-independent; owners can enable downloads for full training environments. | Codex |
| 2026-05-07 | 1 | Phase 1 explicitly follows Gemini Phase 0B audit constraints for NIH. | Official split files are preferred when present, patient overlap is rejected, `pos_weight_c = negative_count_c / positive_count_c` is computed from train labels only, NIH labels are treated as noisy silver-standard labels, and NIH labels are not used for localization. | Codex |
| 2026-05-07 | 1 review fixes | Superseded the Phase 1 no-download default: real NIH training defaults to ImageNet DenseNet121 weights with runtime download enabled; smoke mode overrides downloads off. | Phase 1 requires an ImageNet-pretrained backbone, while no-data smoke runs must remain network-independent. | Codex |
| 2026-05-07 | 1 review fixes | Phase 1 exit conditions are verified by smoke mode only until NIH data is downloaded locally. | Real NIH metrics, the >0.70 macro AUROC target, and meaningful Phase 2 calibration readiness require a follow-up full NIH train/eval run. | Codex |
| 2026-05-07 | 1 review fixes | Smoke evaluation reports null metrics with `WARNING_DO_NOT_USE` instead of synthetic AUROC/AUPRC values. | Synthetic no-data reports must never look like evidence of diagnostic performance or a clinical claim. | Codex |
| 2026-05-07 | 1 review fixes | `dataset_available()` requires NIH metadata plus at least one resolvable referenced image. | Metadata-only partial downloads should fall back clearly to smoke mode instead of failing deep in a loader iteration. | Codex |
| 2026-05-08 | 2 | Phase 2 ships ECE/MCE with 15 equal-width bins by default. | The master prompt specifies 15 bins but not binning; equal-width is the conventional reliability-diagram choice and is exposed in `configs/calibration.yaml`. | Codex |
| 2026-05-08 | 2 | Abstention thresholds start at default `tau_lo=0.30`, `tau_hi=0.70`; Pneumothorax uses `0.20/0.50`, Mass and Nodule use `0.20/0.55`. | Claude's Phase 2 safety design requires per-class thresholds and lower rare-class positive thresholds; these values are starting policy parameters, not tuned on test data. | Codex |
| 2026-05-08 | 2 | Smoke calibration uses NIH-shaped synthetic prevalence: default 0.05, common classes Atelectasis/Cardiomegaly/Effusion/Infiltration at 0.20. | No real Phase 1 logits are available yet; the smoke prior demonstrates calibrator mechanics without clinical-performance claims. | Codex |
| 2026-05-08 | 2 | Temperature scaling is optimized with PyTorch L-BFGS instead of adding SciPy. | PyTorch is already pinned and available; avoiding a new direct dependency keeps Phase 2 environment churn small. | Codex |
| 2026-05-08 | 2 | Selective-risk output is reported as a monotone non-increasing frontier over confidence-band sweeps. | Finite samples can jitter upward at adjacent thresholds; the Phase 2 safety contract requires the published curve to obey the monotone abstention invariant. | Codex |
| 2026-05-08 | 2 review fixes | Superseded envelope-only selective-risk reporting: Phase 2 now reports raw risk, envelope risk, and raw-curve monotonicity violations separately. | Claude review found the envelope masked the Section 4 invariant; auditors need to see both the measured jitter and the published frontier. | Codex |
| 2026-05-08 | 2 review fixes | Added deployed-policy selective-risk curves that sweep the configured per-class `tau_lo`/`tau_hi` bands. | The earlier intrinsic curve was symmetric around 0.5 and did not describe the operational abstention policy. | Codex |
| 2026-05-08 | 2 review fixes | Did not edit `.gitignore` in this pass because the user-provided allowed-file list excluded it, despite Claude's C-1 request. | Phase 2 artifacts remain generated but ignored until `.gitignore` is explicitly authorized for the C-1 fix. | Codex |
| 2026-05-08 | 2 review fixes | `selective_risk_curve` now accepts class names directly, and policy-curve tests exercise a mid-sweep width factor. | This resolves Claude's cosmetic re-review notes without changing Phase 2 behavior or touching Phase 3 surfaces. | Codex |
| 2026-05-08 | 3 | Superseded former VinDr-CXR localization work after the dataset migration review. | VinDr-CXR is now deferred future work only; active localization uses RSNA Pneumonia Detection Challenge 2018. | Codex |
| 2026-05-08 | 3 | RSNA localization loading is strict: positive images must have provider-supplied lung-opacity boxes. | Missing localization labels must not be silently skipped, and NIH labels are not localization ground truth. | Codex |
| 2026-05-08 | 3 | Grad-CAM targets the last discovered `nn.Conv2d` layer and is gated by `not abstained` plus `confidence > confidence_gate`. | Phase 3 requires the last convolutional layer before pooling/head, and explanations must not be generated for abstained or low-confidence predictions. | Codex |
| 2026-05-08 | 3 | Phase 3 overlay artifacts are labeled only as engineering review outputs, with predicted boxes red and ground-truth boxes green. | Grad-CAM heatmaps are not clinically validated evidence; visual output must avoid clinical overclaiming while supporting Gemini audit. | Codex |
| 2026-05-08 | 3 review fixes | Superseded former VinDr consensus-box logic after the RSNA migration. | RSNA provides `Lung Opacity` boxes directly; no VinDr radiologist-consensus path is active. | Codex |
| 2026-05-08 | 3 review fixes | NIH-to-RSNA localization mapping is explicit: NIH `Pneumonia` maps to RSNA `Lung Opacity`; all other NIH labels are unmapped/null. | Cross-dataset localization must not silently treat mismatched label names as zero performance. | Codex |
| 2026-05-08 | 3 review fixes | GradCAM++ is implemented as a secondary method while retaining vanilla Grad-CAM as the default. | The default remains the simpler Phase 3 baseline until reviewed on real RSNA images. | Codex |
| 2026-05-08 | 3 review fixes | `mean_average_precision_at_iou` now computes class-level AP pooled across images; per-image behavior is retained only as `per_image_recall_at_iou`. | Phase 3 mAP@0.5 should follow the VOC/COCO-style class-level aggregation instead of averaging image/class binary outcomes. | Codex |
| 2026-05-08 | 3 review fixes | Phase 3 overlay output defaults to top-level `results/*.png` rather than `results/figures/*.png`. | The current `.gitignore` allowlists direct PNG children of `results/`; nested figures remain ignored unless owner broadens the allowlist. | Codex |
| 2026-05-08 | 3 final fixes | Synthetic Phase 3 overlay samples are generated under `results/overlays/` with an explicit `SYNTHETIC SMOKE - NOT A CLINICAL EVALUATION` banner. | Gemini Phase 3B needs an inspectable overlay corpus before real RSNA data is local, and synthetic visuals must be self-marking to avoid clinical-performance claims. | Codex |
| 2026-05-08 | 3 final fixes | Superseded the prior top-level-only overlay output decision by allowlisting `results/overlays/*.png`. | The Phase 3B audit corpus is clearer as a dedicated directory once owner authorized `.gitignore` scope. | Codex |
| 2026-05-08 | 3 final fixes | `notebooks/02_baseline_results.ipynb` now contains a synthetic-only section that calls the overlay generator and displays the 4x4 grid. | This satisfies the Phase 3 notebook deliverable without claiming real-data localization performance. | Codex |
| 2026-05-08 | 3 final fixes | Superseded former VinDr consensus diagnostics after the RSNA migration. | Active RSNA localization uses provider `Lung Opacity` boxes and does not depend on VinDr consensus annotations. | Codex |
| 2026-05-08 | 3B audit fixes | Added CAM border-artifact metrics and optional heatmap smoothing/border suppression, but left suppression disabled by default. | Gemini flagged border and marker artifacts as real-data risks; suppressing borders by default could hide edge-adjacent pleural findings, so Phase 3B records the diagnostic hooks without changing default evidence. | Codex |
| 2026-05-08 | 3B audit fixes | Added explicit overlay heatmap resize and normalized-bbox denormalization helpers using PIL `(width, height)` image coordinates. | This makes overlay alignment and coordinate conversion testable after Gemini's visual audit confirmed the synthetic overlays were structurally aligned. | Codex |
| 2026-05-08 | 3B final audit | Accepted Gemini's `CONDITIONAL_GO` for Phase 4 engineering entry after RSNA real-image overlay audit. | Synthetic smoke overlays validate plumbing only; real diagnostic quality remains unverified because the classifier checkpoint is smoke-trained. | Codex |
| 2026-05-08 | 3B owner gate | Owner superseded the synthetic-only gate: RSNA Pneumonia Detection real-image overlays and metrics are required for Phase 3B audit. | The project quality bar requires closing the real-data localization gap before Phase 4 safety/UI work. | Codex |
| 2026-05-08 | 3B data unblock | Owner authorized RSNA Pneumonia Detection Challenge 2018 as the active localization dataset. | RSNA provides public lung-opacity bounding boxes suitable for the course-project localization path, while still requiring provider terms and a real-data visual audit before claims. | Codex |
| 2026-05-08 | 3B data unblock | Phase 4 gate requires a real RSNA Pneumonia Detection localization audit, not synthetic overlays. | This preserves the owner's quality bar; no clinical claim is made from the RSNA path. | Codex |
| 2026-05-08 | 3B data unblock | RSNA maps only NIH `Pneumonia` to RSNA `Lung Opacity`; all other NIH labels remain unmapped for RSNA localization. | The RSNA challenge is pneumonia/lung-opacity specific; broader NIH-to-RSNA mapping would be speculative and could create false cross-dataset claims. | Codex |
| 2026-05-09 | 3B data setup | Moved local RSNA dataset files from `src/medguard/data/rsna/` to ignored runtime storage under `data/rsna/` and added a source-tree ignore guard. | Raw medical imaging files must not live inside the importable package tree or appear as commit candidates; `data/rsna/` is the configured non-versioned dataset root. | Codex |
| 2026-05-09 | 3B data setup | `make eval-grounding-rsna` now writes a real-RSNA report but marks it `WARNING_DO_NOT_USE` because the available NIH checkpoint is smoke-trained and produced no confidence-gated Grad-CAM overlays. | The dataset blocker is closed, but model-quality and visual-audit blockers remain; generating heatmaps below the confidence gate would violate Phase 3 explanation policy. | Codex |
| 2026-05-09 | 3B audit fixes | RSNA grounding evaluation now computes confidence with the classifier in eval mode and overlay rendering supports multiple ground-truth opacity boxes per image. | Gemini requested RSNA overlay generation and boundary handling; BatchNorm/train-mode confidence suppressed valid gated CAM candidates, and RSNA rows can contain more than one opacity box for a patient image. | Codex |
| 2026-05-09 | 4A | Phase 4 ships rule-based, classifier-grounded VQA first; QLoRA remains deferred to Phase 4B. | Phase 3B is conditional and the classifier checkpoint is smoke-trained, so fixed templates plus calibrated abstention are safer than free-text VLM generation. | Codex |
| 2026-05-09 | 4A | OOD thresholds start at `std<0.02`, low-frequency FFT ratio `<0.40`, RGB channel-diff mean `>5.0`, and edge-prior chi-square `>1.0`. | These local smoke thresholds catch blank, noise, and natural-color inputs without external services; they must be re-audited on real deployment data. | Codex |
| 2026-05-09 | 4A | The exact diagnosis-refusal template is exempt from the banned-token QA validator while all future non-fixed answers remain blocked on clinical tokens. | Claude's fixed refusal answer intentionally contains terms like diagnoses and treatment; rejecting that exact refusal would break the safety path the spec requires. | Codex |
| 2026-05-09 | 4A | The Phase 4 API has no authentication and binds to localhost-only defaults. | The demo is a local research artifact and must not be exposed on a public network without a future auth and deployment review. | Codex |
| 2026-05-09 | 4A review fixes | Rolled back the accidental `RONA` artifact; RSNA Pneumonia Detection Challenge 2018 is the active localization dataset, and VinDr-CXR is deferred future work only. | Codex misinterpreted a dataset migration instruction, inventing a dataset name while leaving stale VinDr active paths. Corrected to establish RSNA Pneumonia Detection as the core localization evaluation target and preserve strict constraints before any QLoRA work. | Gemini |
| 2026-05-09 | 4A review fixes | Default Phase 4A smoke probabilities now sit inside abstention bands for every NIH class. | Smoke-mode demos must not fabricate confident positives from unvalidated weights; positive evidence paths remain testable only through explicit fixed-probability test services. | Codex |
| 2026-05-09 | 4A review fixes | OOD lateral-view warning-only decisions are treated as hard VQA abstentions in Phase 4A. | This tightens Claude's warning-only design for the local demo: unsupported-view images should not receive finding answers while the view classifier path remains unvalidated. | Codex |
| 2026-05-09 | 4A review fixes | `view_classifier_path` in OOD config now raises unless a callable view classifier is wired into `detect_ood`. | Silently defaulting configured view-classifier paths to PA created a false sense of view-gate coverage. | Codex |
| 2026-05-09 | 4A owner correction | Retained VinDr-CXR files as explicitly flagged deferred future work rather than deleting them. | Owner explicitly instructed: do not delete VinDr surfaces; keep them flagged for later use. Files remain importable for a future access-grant/reactivation pass, but RSNA Pneumonia Detection remains the active localization/evidence dataset and VinDr is not a Phase 4A dependency. | Codex |
| 2026-05-09 | 3R | Started RSNA Pneumonia Detection real-validation pass with negative cases included for binary Pneumonia classification metrics. | Phase 3R evaluates only NIH `Pneumonia` against RSNA `Lung Opacity`; all outputs remain marked `WARNING_DO_NOT_USE` while the available NIH checkpoint is smoke-trained. | Gemini |
| 2026-05-09 | 3R | RSNA evaluation uses a bounded 1024-record validation subset by default and clears prior RSNA overlay outputs before regeneration. | This keeps `make eval-grounding-rsna` feasible locally, produced 20 confidence-gated overlay samples in the current smoke-checkpoint run, and prevents stale overlays from mixing with the current report; the report records `n_dataset_records` and `n_evaluated` so scope is explicit. | Codex |
| 2026-05-10 | 3R review fixes | Surfaced Phase 3R RSNA smoke-checkpoint metrics in the README scorecard and drafted evidence-backed model card/datasheet safety sections. | Opus approved Phase 3R with conditions: user-facing docs must expose generated RSNA numbers with `WARNING_DO_NOT_USE`, explain below-chance AUROC as smoke-checkpoint behavior, and keep Phase 4B deferred until a real NIH checkpoint refreshes calibration and RSNA validation. | Codex |
| 2026-05-09 | standing owner policy | At the end of every phase, after required checks pass, Codex should make a proper git commit and push it to GitHub. | Owner requested GitHub pushes as the default phase-closeout behavior so reviewers and future phases always start from a durable remote state. | Owner |
| 2026-05-10 | 4B design | Phase 4B VLM/QLoRA design specification produced by Claude Opus 4 as design owner. Key invariant: the calibrated DenseNet121 classifier remains the primary decision-maker; the VLM is a presentation layer that reformulates classifier decisions into natural language. VLM output that conflicts with the classifier is rejected and falls back to rule-based. Zero-shot baseline is mandatory before fine-tuning. All VLM answers pass through six post-inference safety filters including banned-token, hallucination, classifier-consistency, evidence-gate, confidence-gate, and length checks. | Claude designed; Codex implements; Claude reviews. Carry-forward fixes M-1, M-2, m-1, m-2 from catch-up review must be applied first. | Claude |
| 2026-05-10 | 4B implementation | Implemented Phase 4B as an optional, config-driven VLM/QLoRA extension with rule-based VQA preserved as the default fallback. | The VLM is guarded by structured-output validation, banned-token filtering, hallucination checks, classifier consistency, evidence gating, abstention gating, and length limits. Local QLoRA training writes an explicit blocker report when GPU/dependencies/data are unavailable and does not claim model quality. | Codex |
| 2026-05-10 | 4B review fixes | Added explicit placeholder documentation to the local QLoRA adapter-initialization path and made successful-run smoke warnings depend on `vlm.training.classifier_checkpoint_is_smoke`. | Claude approved Phase 4B with conditions. The `bitsandbytes` optional-dependency fix requires `pyproject.toml`, which was outside the owner's allowed-file list for this turn. | Codex |
| 2026-05-10 | 4B M-A fix | Moved `bitsandbytes>=0.43.0` from core `dependencies` to `[project.optional-dependencies].vlm` in `pyproject.toml`. Install via `pip install -e ".[vlm]"` when GPU+CUDA available. | Completes final review condition M-A. All three conditions (M-A bitsandbytes, M-B training stub comment, M-C F-3 regex) are now resolved. Phase 4B closeout is unblocked. | Claude |
